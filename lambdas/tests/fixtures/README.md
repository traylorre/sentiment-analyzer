# Test Fixtures

Sample data for testing. Real Twitter format is unstable and may include additional fields.

- `twitter_sample.json` - Real Twitter API response (circa 2011)

Tests use simplified fixtures in `conftest.py` that only include required fields.
